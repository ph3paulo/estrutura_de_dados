#include <iostream>
#include <vector>

using namespace std;

void print(vector<int> PiramidRed){

}

int main(){
    int size {10}, input{};

    vector<int> PiramidRed(size);

    for(int i = {}; i < size; i++){
        cin >> input;
        PiramidRed.push_back(input);
    }
    

    return 0;
}
